{
  "dependencies": {
    "compression-next": "^1.0.3",
    "express": "^4.21.2",
    "npm-run-all": "^4.1.5",
    "socket.io": "^4.8.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2"
  },
  "type": "module",
  "devDependencies": {
    "@types/compression-next": "^1.0.3",
    "@types/express": "^5.0.0"
  },
  "scripts": {
    "start": "npm-run-all build --parallel watch:build watch:server --print-label",
    "watch:build": "tsc --watch",
    "watch:server": "nodemon ./build/backend/index.mjs --watch './build'",
    "build": "tsc"
  }
}
